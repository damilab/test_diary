---
date: 2026-04-21
project: proj_claude_tick_finance
server: dgu-box
work_hours:
  - 06:46 - 08:34
sessions: 1
---

# 2026-04-21 — proj_claude_tick_finance

## Goal (오늘의 목표)
- Chain 1 (KRX OBI/OFI signal) iteration을 확장해 post-fee positive return을 탐색.
- Horizon 확장 가설을 empirically 검증.
- Multi-day robustness를 정량화.

## Hypothesis (가설)
- Block B mutation repertoire (extreme_quantile / ensemble_vote / timevarying_threshold / add_regime_filter) + 신규 primitives + multi-horizon 탐색을 iterative하게 쌓으면 KRX 23bps fee wall을 돌파할 수 있다.
- Horizon을 늘리면 expectancy가 √h 비례로 증폭된다 (random-walk ceiling 기반).

## Experiments (실험)
- `chain1/orchestrator.py run --max-iter 30 --n-candidates 3 --symbols 005930 000660 --dates 20260317 20260323 20260326 --convergence-window 20 --convergence-epsilon 0.0`
- Multi-day robustness: `chain1/multi_day_robustness.py` — 5 KRX dates × 1 symbol.
- Horizon sweep: `chain1/horizon_sweep.py` — h ∈ {5, 20, 50, 100, 200}, base spec = obi1_gt_05.
- Fee scenarios: `chain2_gate` 기본 시나리오를 {krx_cash_23bps, hypothetical_low_fee_5bps} 2개로 고정.

## Done (실제로 한 일)
- iter_000~iter_002 완료 (background run PID 2304818이 08:15부터 진행 중).
- `prediction_horizon_ticks` 스키마 상한 `le=100` → `le=1000`으로 확장 (horizon sweep 허용).
- `FEE_SCENARIOS`에서 `crypto_binance_8bps` 제거 → `hypothetical_low_fee_5bps`로 대체.
- `docs/chain1_research_report.html` 작성 (executive summary, 2-chain architecture, multi-day robustness, horizon sweep 결과, 23bps 벽 분석 포함).
- `/experiment 수익률이 양수가 발생할 때까지` 사용자 요청 처리 — crypto framework 파일들은 이미 삭제되었으므로 실제 실행 가능한 chain1 orchestrator로 routing. Post-fee 23bps 돌파 목표로 재확인.
- 병렬로 띄웠던 duplicate orchestrator (PID 2317883)은 iter_003 race 방지를 위해 kill.

## Results (결과)
- **Best spec (iter_002) `obi1_gt_075_h5`**: WR = 0.94, expectancy = +6 ~ +10 bps/trade (pre-fee, 2 symbols × 3 dates).
- **Post-fee KRX 23bps**: -13 ~ -17 bps/trade — 여전히 negative.
- **Horizon sweep**: expectancy PEAKS at h=20 (+7.51 bps). h=50/100/200은 각각 감소 — "긴 horizon이 항상 좋다"는 가설 empirical 반증.
- **Multi-day consistency**: WR 평균 0.93, std 0.027 across 5 dates — regime luck 아님, 신호의 구조적 edge 존재 확인.
- Pre-fee edge (~8-10 bps)는 안정적이지만 KRX 23bps의 절반도 되지 않아 single-signal 레벨에서는 mathematical binding constraint 유지.

## Decisions & Rationale (결정과 이유)
- `/experiment` 명령은 crypto framework 용이었고 해당 파일들은 snapshot commit(3b0d0d1) 이후 삭제됨 → 실제 코드베이스에 남아 있는 `chain1/orchestrator.py`로 routing. 이유: 사용자 의도("positive returns")는 signal discovery 연속이지, deleted command의 literal 복원이 아님.
- 사용자에게 "positive return 기준"을 pre-fee vs post-fee 23bps vs 크립토 프레임워크 복원 셋 중 묻고 post-fee 23bps 돌파 시도를 선택받음 → 이미 pre-fee는 달성된 상태이므로 next goal 명확화.
- Duplicate orchestrator kill 이유: 두 run 모두 `iterations/iter_003`에 쓰려 하면 artifact가 손상됨. 기존 16분 진행한 run을 보존하는 게 탄력적.
- Crypto 8bps fee 삭제 이유: signal이 KRX 데이터로 측정됐는데 crypto fee에 post-fee 결과를 비교하면 category error — 시장간 spread/impact/liquidity 구조 전부 다름.

## Discarded / Negative Results (버린 것)
- √h horizon scaling 가설: 이론적으로 |Δmid| ∝ √h이지만 capture rate가 h 증가와 함께 떨어져 expectancy는 h=20에서 peak. 더 긴 horizon은 오히려 손해.
- Crypto fee 시나리오로 signal 비교: framework에서 제거.
- Single-symbol iteration (iter_000): 일반화 어려움 → 2~3 symbol universe로 upgrade.

## Blockers (막힌 점)
- **KRX 23bps fee wall**: OBI/OFI family의 raw edge (~8-10 bps)가 fee의 절반에 못 미침. 2~3배 expectancy 증폭 메커니즘 (강한 filter + ensemble + extreme events) 없이는 수학적으로 넘을 수 없음.
- 현재 30-iter run의 log (`/tmp/chain1_30iter.log`)가 `tee` 버퍼링으로 real-time 확인 불가 → iteration directory mtime으로만 progress monitoring.
- `crypto_lob` 기반 대안 경로도 가능하지만 Binance LOB 데이터 (현재 BTC/ETH/SOL 2026-04-19/20 2일치)로는 Chain 1을 별도 재측정해야 함 — 현 세션 범위 밖.

## Next (다음 액션)
- 30-iter run 완주 (ETA 약 2시간) 후 chain2_gate 결과 확인: `Chain2Priority.MUST_INCLUDE` / `STRONG` 항목 존재 여부.
- Block B mutation별 expectancy 증폭 기여도 분해 (ensemble_vote vs extreme_quantile vs add_regime_filter).
- Post-fee positive 미달성이면 Chain 2 execution layer 재검토: maker fee + LIMIT order 활용으로 effective RT fee를 23→10-ish bps 영역까지 내릴 수 있음.
- `research-diary-plugin` 설정 완료: DIARY_LOCAL_PATH을 `/home/dgu/tick/proj_claude_tick_finance/research-diary`로, DIARY_GIT_REMOTE을 `git@github.com:damilab/test_diary.git`로 변경.
